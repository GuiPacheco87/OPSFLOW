import "./globals.css";
export const metadata={title:"OpsFlow — trabalho em movimento"};
export default function Layout({children}:{children:React.ReactNode}){return <html lang="pt-BR"><body>{children}</body></html>}
